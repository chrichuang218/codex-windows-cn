# v1 is a Chinese install and update assistant

v1 will ship as a Chinese install and update assistant: it preserves the five main user-visible paths from the reference project: install, proxy launch, check/update, uninstall, and launcher self-update. Diagnostics, repair flows, network checks, and other toolbox-style enhancements are intentionally deferred so the first release can prove the core experience without expanding product scope.
