# Use Tauri, React, and TypeScript for the UI

The desktop UI will move to Tauri with React and TypeScript instead of continuing with Slint, switching to egui, or using Electron. This keeps the Rust system logic lightweight and reusable while giving the project enough frontend control to deliver a polished Chinese interface without Electron's size and runtime cost.
